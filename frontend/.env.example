VITE_API_BASE_URL=/api

# Same Google OAuth Client ID as backend/.env's GOOGLE_CLIENT_ID (this one is
# safe to expose in frontend code — Google Sign-In client IDs are public by
# design; the backend independently verifies every token's signature).
VITE_GOOGLE_CLIENT_ID=
